// Este archivo se puede reemplazar durante la compilación usando `fileReplacements`.
// `ng build --prod` reemplaza `environment.ts` por `environment.prod.ts`.
// La lista de archivos de reemplazos se encuentra en el archivo `angular.json`.
export const environment = {
    production: false,
    apiUrl: 'http://localhost:8080', //window['env']['ApiUrl'] || 'http://localhost:8080'
};
